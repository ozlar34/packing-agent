# Copy to .env (gitignored) and fill in. NEVER commit the real .env.
# Google AI Studio API key — get one at https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Tells the google-genai / ADK SDK to use the AI Studio key (not Vertex AI).
GOOGLE_GENAI_USE_VERTEXAI=FALSE
